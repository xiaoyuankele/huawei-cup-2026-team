# Q2 conditional bridge decision

The user confirmed that A and B have no unique ID linking individual samples. The formal row-level A-B joint model is therefore not identifiable from the current files. This run freezes a conditional, domain/scenario-level bridge and keeps the Q1-derived Q/p interface separate from the native B6/B7 `Q_score` field.

# Q1 scale feature table

- run_id: `q1-scale-feature-table-20260924-r01`
- task: `T-Q1-003-MIXTURE`
- source: `data/origin/real_attachments/A_data_value/regmix_tables`
- outputs: normalized wide and long tables with scale, log10 scale, domain proportions, and expected per-domain token counts

The source attachments were read only. Expected token counts are derived allocations (`normalized proportion * total scale tokens`), not independently observed integer counts. This run does not fit a Loss model or change A4-A15 data roles.

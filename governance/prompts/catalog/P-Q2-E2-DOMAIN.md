# P-Q2-E2-DOMAIN@v1.0.0

状态 draft；执行前登记的任务级模板，不声称覆盖每次底层工具调用。

执行预设 E2 方案：冻结参数与A4配方输入，求有向替代支持范围和有限效应；用有凸组合见证的四角差检验模型交互；独立标记截断压力测试。输入只允许已发布特征，不读观测Loss、不拟合、不伪造联合观测。所有方向、不可行结果和限制必须保留；输出可复现实验、报告、图、DRAFT主张和handoff。独立评审未完成时保持REVIEW_BLOCKED。

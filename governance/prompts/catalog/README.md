# 提示词模板目录

这里存放可以公开的提示词模板。每个模板使用 `P-<DOMAIN>-<编号>.md` 命名，并在 YAML front matter 中写明 `prompt_id`、版本、工作包 Owner、Peer Reviewer、输入、输出和验收条件。模板不绑定固定 Actor。

实际调用记录放在 `governance/prompts/runs/` 或受控本地目录；涉及未公开题目和原始数据的正文不能提交公开仓库。

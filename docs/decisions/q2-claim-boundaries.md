# 问题二当前主张边界

## 当前可以报告

- B1 内部按参数规模分组留出的经典 N-D 经验基线；
- B2/B4/B5 的来源分层外部诊断，附带跨来源限制；
- B6/B7 半合成数据中的条件质量敏感性，明确 B7 包含 B6；
- A16 映射覆盖率、未映射质量权重和 Q 情景区间；
- B8 的方向冲突、来源缺口和 calibrated/extrapolated 分层事实；
- 迁移流程已完成的 P0–P6 证据包及剩余解锁条件。

## 当前不能报告

- Q1 已经得到最终、验收通过的统一标量质量分数；
- A 与 B 已建立逐行质量—损失关系；
- Q1 候选分数可以直接替代 B6/B7/B8 的 `Q_score`；
- B8 的反向方向可以通过反转 Q、删点或单调约束修复；
- 质量对 Loss 的因果效应；
- 适用于所有来源、规模和配比的正式广义标度律。

正式文稿中的每条问题二主张都应回链到 `run_id`、输入哈希、数据角色、证据等级和本边界表。

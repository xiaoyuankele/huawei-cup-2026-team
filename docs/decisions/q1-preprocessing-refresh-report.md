# T-Q1-002 新运行交接

新运行：q1-indicator-normalization-20260924-r02；旧运行保留为 q1-indicator-normalization-20260923-r01。此次在独立交付目录重建归一化矩阵，并生成相互一致的新指标字典、统计、审计表、敏感性表与 manifest。实际运行读取原附件，未改写、复制原始数据。

全部 272,505 条记录保留，A1 fit 40,919、holdout 10,311；16 项可用，9 项未决方向保持空列。原始文件运行前后哈希一致。主评分的 16 项 A1 p01/p99 参数与之前记录一致。所有新 manifest 声明的五项产物哈希经核对匹配。

旧 manifest 与当时目录中四项文件的哈希差异保留为反馈记录，历史原因未确认；新运行不被冒充为旧运行的原产物。具体哈希见 governance/feedback/records/FB-20260924-B-PREPROCESS-PROVENANCE.json。

源脚本增加 --raw-root 只读外部路径参数；方向声明补充领域适用性限制；部分描述性漂移摘要沿用原脚本前 5,000 个有效值的规则，已标记限制。新模型运行另提供全量归一化零/一边界占比。本任务输出不包含综合评分，评分结果由 T-Q1-004 单独登记。

附加来源核对：新建 q1_X_norm_v2.csv 的字节 SHA256 与旧 manifest 登记的缺失矩阵哈希完全一致；因此已恢复该矩阵内容。其他四项旧元数据文件的历史哈希差异仍保留反馈，新运行的全部产物使用一致的新清单。

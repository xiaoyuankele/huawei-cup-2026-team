# E4：B9/B10 百亿以上外推审计

task_id: T-Q2-E4；run_id: q2-e4-b9-b10-extrapolation-20260926-r01。本计划在执行前登记，针对题目要求“结合 B9/B10 讨论百亿以上外推”。

## 可用输入和严格边界

发布仓库没有逐行 B9/B10 原始CSV，只保留了先前只读审计 q2-b9-b10-extrapolation-audit-20250925-r01/metrics.json：B9 有132条大模型元数据，B10有128条估算基线；两者参数范围均为100–10000B，token范围最高36000B。B10的估算Loss不进入拟合、留出误差或真值对照。

E4只读取冻结v1参数、B9/B10审计摘要和E3可行质量路径上限，建立N/D高尺度网格。输出三类结果：B1支持域内外的明确标记；冻结模型的形式趋势、边际效用和弹性；质量情景在大尺度下的条件影响。任何“预测—B10 Loss”的行级差值都不生成。

## 预设计算

1. N=[0.1,1,10,100,1000,10000]B，D=[0.1,1,10,100,1000,10000,36000]B，按B1边际支持 [0.070542, 11.965825]、[0.134, 299.893] 标记是否越界。
2. 以参考配方和两条E3质量路径计算 lambda_Q=0,0.5,1；feasible_99pct 是99%凸包安全路径，formal_coordinate 只作独立质量坐标压力情景。
3. 保存 Loss、dL/dlogN、dL/dlogD、总Loss弹性及相对B1边界的外推倍率；检查 N、D 单调下降和渐近下界。
4. 对 alpha、beta 各做±10%确定性敏感性，不解释为置信区间。
5. 单独报告 B9/B10 的覆盖范围、外推距离、重复名称覆盖、缺失单元和来源字段不完整；不将审计摘要中的计数当作统计样本量。

## 验收

输入哈希不变；模型回代与解析梯度一致；第二目录重跑CSV逐字节一致；图形通过既有 Python/nature-figure 自查。任务和结果保持 REVIEW_BLOCKED，不升级为跨尺度实证验证。
